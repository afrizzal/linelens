---
phase: 02-oee-engine
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified: [packages/db/prisma/schema.prisma, packages/db/prisma/migrations/*, apps/worker/src/derive/*, apps/worker/src/notify.ts, apps/worker/test/*]
autonomous: true
requirements: [ENG-02, ENG-04]
must_haves:
  truths:
    - "Every loss in the plant lands in ONE loss_event ledger, in lost-time units, tagged to exactly one Six Big Loss, retaining machine/line/time identity"
    - "Changeover splits: time up to target → PLANNED_STOPS; overage → UNPLANNED_STOPS (CO-OVERAGE) — and the policy is configurable"
    - "Small stops and slow cycles land as PERFORMANCE losses; small stops come from ALARM events, slow cycles are the residual"
    - "Derivation is idempotent: re-deriving the same raw events produces the same ledger"
  artifacts:
    - path: "apps/worker/src/derive/intervals.ts"
      expected: "close-on-next-event state_interval derivation"
    - path: "apps/worker/src/derive/losses.ts"
      expected: "loss_event emission rules for all six categories"
  key_links:
    - from: "machine_event"
      to: "state_interval + loss_event"
      via: "deterministic derivation, replayable from raw"
---

<objective>
Build the derivation core: machine_event → state_interval (close-on-next-event) → the loss_event attribution ledger with the verified Six Big Losses classification rules. This ledger is the single source feeding the waterfall, the Pareto, AND the DIFOT drill-down — its schema and correctness carry the whole product.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/00-domain-research.md (§2 changeover/overage rules, §3 taxonomy — the rules encoded here)
@.planning/research/ARCHITECTURE.md (§ derivation pattern, § loss ledger)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derived-state schema (state_interval, loss_event, config)</name>
  <files>packages/db/prisma/schema.prisma, packages/db/prisma/migrations/*</files>
  <action>
    Add models (hand-review migration SQL):
    - `StateInterval { id BigInt, machineId, lineId, state, startTime DateTime, endTime DateTime?, reasonCode String?, injected Boolean @default(false), sourceEventId BigInt  @@index([machineId, startTime]) @@index([lineId, startTime]) }` — endTime NULL = open/live interval.
    - `LossEvent { id BigInt, machineId, lineId, factor, category, reasonCode, windowStart DateTime, windowEnd DateTime, lostTimeSec Float, lostUnits Float @default(0), injected Boolean @default(false), stateIntervalId BigInt?, sourceEventId BigInt @default(0), shiftDate String?, shiftId String?  @@unique([machineId, category, reasonCode, windowStart, sourceEventId]) @@index([lineId, windowStart]) @@index([category, windowStart]) }` — the attribution ledger. `sourceEventId` is NON-NULLABLE with 0 as the explicit no-source sentinel (Postgres treats NULLs as distinct in unique indexes — a nullable column would silently break replay-idempotency): interval-derived rows (rules 1/2/4) use 0; ALARM-microstop rows carry the ALARM MachineEvent id; reject rows carry the COUNTS MachineEvent id. `injected` propagates from the source interval (rules 1/2: copy interval.injected; all other rules: false) — 04-02's drill-down reads it directly from the ledger.
    - `EngineConfig { id Int @id @default(1), changeoverAsPlanned Boolean @default(true) }` — the configurable ENG-04 policy.
    - `MachineCursor { machineId @id, lastEventId BigInt, lastSeq Int }` — derivation progress (restart resumes; full re-derive = delete derived rows + reset cursors).
  </action>
  <verify>Migration deploys from scratch on disposable postgres:18; typecheck clean.</verify>
  <done>Derived-state tables exist with idempotency keys.</done>
</task>

<task type="auto">
  <name>Task 2: Interval derivation (close-on-next-event)</name>
  <files>apps/worker/src/derive/intervals.ts</files>
  <action>
    Poll loop (every 200ms real): per machine, read machine_event where id > cursor ORDER BY seq, process in order, one transaction per batch:
    - STATE_CHANGE ev → `UPDATE state_interval SET end_time = ev.simTime WHERE machine_id = ? AND end_time IS NULL`; then INSERT new open interval (state, startTime=ev.simTime, reasonCode, injected from meta). Guard: if new state == open interval's state, skip (idempotent replay safety).
    - First event for a machine bootstraps its first interval.
    - COUNTS / ALARM events pass through to Task 3 handlers in the same ordered scan (single pass, single writer).
    - Advance MachineCursor in the same transaction (exactly-once effect).
    ALL time math uses ev.simTime. `Date.now()` is BANNED in apps/worker/src/derive/** (standing rule; enforce with a vitest static check: read the dir's files and fail on /Date\.now|new Date\(\)/ outside comments).
  </action>
  <verify>Unit test with an in-memory event fixture: EXECUTE→DOWN→EXECUTE→CHANGEOVER→EXECUTE yields 5 intervals with exact start/end times, last one open; replaying the fixture leaves rows unchanged.</verify>
  <done>Correct, idempotent machine timeline.</done>
</task>

<task type="auto">
  <name>Task 3: Loss ledger emission (the Six Big Losses rules)</name>
  <files>apps/worker/src/derive/losses.ts</files>
  <action>
    EXACT RULES (verified — do not improvise):
    1. **DOWN interval closes** → one loss_event: factor AVAILABILITY, category UNPLANNED_STOPS, reasonCode = interval.reasonCode, window = interval, lostTimeSec = duration.
    2. **CHANGEOVER interval closes** → read machine.changeoverTargetMin (master data) and EngineConfig.changeoverAsPlanned:
       - policy true (default): `min(dur, target)` → PLANNED_STOPS (reason CO-PRODUCT/CO-CLEAN from interval); `max(0, dur − target)` → a SECOND loss_event, UNPLANNED_STOPS, reason CO-OVERAGE, windowStart = start+target. (Verified planned→unplanned overage transition, §2.)
       - policy false: single loss_event PLANNED_STOPS for the full duration (no overage split). Either way changeover time reduces Run Time (it is inside PPT).
    3. **ALARM MICROSTOP** → loss_event: factor PERFORMANCE, category SMALL_STOPS, reason = event.reasonCode, windowStart = simTime − durationSec, windowEnd = simTime, lostTimeSec = durationSec, sourceEventId = the ALARM MachineEvent id.
    3b. **Product freshness**: when a COUNTS event carries a productId different from Machine.currentProductId, update the Machine row in the same scan transaction — 04-01's capacity sizing and 04-02's line resolution read this column and must never see a stale product after a changeover rotation.
    4. **Slow cycles (residual, per closed shift-bucket)**: when a (machine, shiftInstance) window fully elapses (shift end < newest processed simTime), compute from that shift's data:
       `runTimeSec = PPT − Σ(DOWN) − Σ(CHANGEOVER)` (intervals clamped to the shift window AND excluding any overlap with break windows — breaks are already outside PPT, so counting them inside a DOWN/CHANGEOVER interval would double-subtract; PPT from contracts calendar);
       `pLossTotalSec = max(0, runTimeSec − Σ(idealCycleTimeSec × (goodDelta+rejectDelta)))` over COUNTS in-window (use each event's own ICT — product changes mid-shift are thereby handled);
       `slowCyclesSec = max(0, pLossTotalSec − Σ smallStopsSec-in-window)` → upsert ONE loss_event per (machine, shift): SLOW_CYCLES, reason SL-SPEED, window = shift window, shiftDate/shiftId set.
    5. **Rejects (per COUNTS event with rejectDelta>0)** → loss_event factor QUALITY: category STARTUP_REJECTS if rejectReason==RJ-STARTUP else PRODUCTION_REJECTS; reason = rejectReason; lostUnits = rejectDelta; lostTimeSec = rejectDelta × idealCycleTimeSec; window = [simTime, simTime]; `sourceEventId` = the COUNTS MachineEvent id. Replaying the same raw event upserts the same ledger row (the Task 1 unique key includes sourceEventId) — never accumulate-on-conflict, that is not replay-safe.
    Tag every loss_event with shiftDate/shiftId via contracts.shiftInstanceAt(windowStart).
  </action>
  <verify>Unit tests per rule with hand-built fixtures, incl.: changeover 20 min vs target 15 → exactly two rows (15 min PLANNED_STOPS + 5 min CO-OVERAGE UNPLANNED_STOPS); policy=false → one 20-min PLANNED_STOPS row; microstop alarm 45s → SMALL_STOPS 45s; full-shift fixture where pLossTotal=20min and microstops=8min → SLOW_CYCLES 12min; replay every fixture → identical ledger.</verify>
  <done>Ledger rules implemented exactly as verified; replay-idempotent.</done>
</task>

<task type="auto">
  <name>Task 4: Coalesced NOTIFY</name>
  <files>apps/worker/src/notify.ts</files>
  <action>
    After each derivation batch that changed anything: collect affected lineIds; coalesce 250ms (timer); `pg_notify('linelens', json { kinds: ['telemetry'], lineIds })` — IDs only, payload < 500 bytes (8KB NOTIFY cap, ARCHITECTURE §SSE). Use a dedicated raw `pg` connection (not Prisma) for NOTIFY.
  </action>
  <verify>Integration: run derivation over a burst of 500 events → LISTEN client receives ≤ ~8 notifications (coalescing works), each valid JSON.</verify>
  <done>Change signal exists for Phase 3's SSE without payload bloat.</done>
</task>

</tasks>

<verification>
- All fixture tests green; static no-wall-clock check green.
- Live: compose up 10 real minutes (speed 60 → 10 sim-hours) → state_interval timeline per machine has no overlaps/gaps (SQL check: no interval whose start < previous end), loss_event has rows in ≥5 of 6 categories.
</verification>

<success_criteria>
One auditable ledger, exact verified classification semantics, replay-idempotent (ENG-02, ENG-04 done). The DIFOT join keys (lineId, window, lostUnits) are already in place for Phase 4.
</success_criteria>

<output>
Write `.planning/phases/02-oee-engine/02-02-SUMMARY.md`. Note any schema deviation (e.g. the reject unique-key correction) explicitly.
</output>
