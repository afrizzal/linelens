---
phase: 02-oee-engine
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: [packages/db/prisma/schema.prisma, packages/db/prisma/migrations/*, packages/db/src/*, packages/db/seed/*, apps/worker/src/ingest.ts, apps/worker/src/main.ts]
autonomous: true
requirements: [ENG-01]
must_haves:
  truths:
    - "The worker is the sole MQTT consumer and sole Postgres writer; raw machine_event rows are lossless and idempotent (replaying the same events changes nothing)"
    - "Prisma 7 connects via @prisma/adapter-pg (driver adapter is mandatory — a naive setup will not connect)"
    - "Master data (lines, machines incl. changeoverTargetMin, products, shifts) is seeded from plant.config.json — one source of truth"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      expected: "master data + machine_event + sim_clock models"
  key_links:
    - from: "apps/worker/src/ingest.ts"
      to: "machine_event"
      via: "idempotent upsert on natural key (machineId, seq)"
---

<objective>
Stand up the database layer (Prisma 7 + migrations + seed-from-config) and the worker's ingestion path: subscribe Mosquitto, validate against @linelens/contracts, persist raw `machine_event` losslessly and idempotently. No derivation yet — raw truth first, replayable forever.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/ARCHITECTURE.md (§ table sketch, § single-writer)
@.planning/research/STACK.md (§ Prisma 7 driver adapter, § MQTT gotchas)
@.planning/phases/01-foundation-living-plant/01-02-PLAN.md (event contract)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma 7 schema + migrations + client factory</name>
  <files>packages/db/prisma/schema.prisma, packages/db/prisma/migrations/*, packages/db/src/client.ts, packages/db/prisma.config.ts</files>
  <action>
    Prisma 7 setup per STACK.md: `prisma.config.ts` holds the datasource url (NOT schema.prisma — AIDA precedent); client factory in `src/client.ts` MUST use the driver adapter:
    ```ts
    import { PrismaPg } from '@prisma/adapter-pg';
    import { PrismaClient } from '../generated/prisma/client';
    export const createDb = (url = process.env.DATABASE_URL!) => new PrismaClient({ adapter: new PrismaPg({ connectionString: url }) });
    ```
    Models (map to the ARCHITECTURE.md sketch; use @@map to snake_case table names):
    - `Line { id, name }` · `Product { id, name, idealCycleTimeSec Float }`
    - `Machine { id, lineId, name, profile, changeoverTargetMin Float, startupWindowMin Float, currentProductId }`
    - `Shift { id, name, startMin Int, endMin Int, breaks Json }` (mirror of contracts ShiftDef; seeded, read by SQL)
    - `ReasonCode { code @id, label, category }` (from contracts reasons)
    - `MachineEvent { id BigInt autoincrement, machineId, lineId, kind, simTime DateTime, ingestedAt DateTime @default(now()), seq Int, state String?, reasonCode String?, goodDelta Int?, rejectDelta Int?, rejectReason String?, idealCycleTimeSec Float?, productId String?, durationSec Float?, meta Json?  @@unique([machineId, seq]) @@index([machineId, simTime]) }` — append-only, THE replay source.
    - `SimClock { id Int @id @default(1), epochSimMs BigInt, startedAtRealMs BigInt, speed Float, pausedAtRealMs BigInt? }`
    Hand-add to the migration SQL: `CREATE FUNCTION sim_now() RETURNS timestamptz` reading SimClock (ARCHITECTURE.md snippet) — SQL views in 02-03 need it. Review generated migration SQL by hand before commit (AIDA's standing Prisma-diff lesson).
  </action>
  <verify>`prisma migrate deploy` against a disposable postgres:18 container from scratch succeeds; `sim_now()` callable; typecheck clean.</verify>
  <done>Schema + migrations + adapter-based client exist.</done>
</task>

<task type="auto">
  <name>Task 2: Seed from plant.config.json</name>
  <files>packages/db/seed/seed.ts</files>
  <action>
    Seed script (idempotent upserts): read `plant.config.json` via the contracts zod schema → upsert Products, Lines, Machines (incl. per-machine changeoverTargetMin = profile/override value; startupWindowMin), Shifts, ReasonCodes (from contracts), SimClock row (epochSim from env or config, speed from env SIM_SPEED). Wire `pnpm db:seed`; worker runs migrate+seed on startup if tables are empty (compose-friendly) — guard with advisory lock to stay single-writer-safe.
  </action>
  <verify>Run seed twice → identical row counts (idempotent). Machines carry correct changeoverTargetMin per profile.</verify>
  <done>plant.config.json is the single source of master data, mirrored to DB.</done>
</task>

<task type="auto">
  <name>Task 3: Worker MQTT ingestion (idempotent raw persistence)</name>
  <files>apps/worker/src/ingest.ts, apps/worker/src/main.ts</files>
  <action>
    - mqtt@5 client per STACK.md rules: handlers before connect; clientId `linelens-worker` (FIXED — session identity); `clean: false`; subscribe `TELEMETRY_SUBSCRIPTION` QoS 1. Reconnect: rely on mqtt.js auto-reconnect; log gaps.
    - On message: zod-parse via contracts `TelemetryEvent` (invalid → log + drop to a `quarantine` log table? NO — keep v1 lean: log.warn with topic+error, drop; note in SUMMARY). Map to MachineEvent columns; `createMany({ skipDuplicates })` in micro-batches (flush every 100ms or 500 events — accelerated clock volume, PITFALLS batching note) relying on the `(machineId, seq)` unique key for idempotency.
    - main.ts: migrate/seed-if-empty (Task 2), start ingestion, graceful SIGTERM (flush batch, end client with `end(false)` so the broker keeps QoS1 session).
  </action>
  <verify>Integration test (Testcontainers postgres + compose mosquitto or aedes-in-process): publish 1,000 contract events incl. 200 duplicated (same machineId+seq) → exactly 800 machine_event rows; re-run the same publish → still 800 (idempotent). Kill+restart worker mid-stream → no loss (QoS1 redelivery) and no dupes.</verify>
  <done>Lossless, idempotent, single-writer raw ingestion proven.</done>
</task>

</tasks>

<verification>
- All tests green; fresh `docker compose up` → worker migrates, seeds, ingests; `SELECT count(*) FROM machine_event` grows.
- Only the worker writes to Postgres (web has no write path yet; keep it that way).
</verification>

<success_criteria>
Raw telemetry lands losslessly and idempotently in `machine_event` with master data seeded — the replayable foundation the whole credibility gate stands on (ENG-01 done).
</success_criteria>

<output>
Write `.planning/phases/02-oee-engine/02-01-SUMMARY.md`.
</output>
