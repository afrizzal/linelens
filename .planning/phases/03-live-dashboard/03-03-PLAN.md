---
phase: 03-live-dashboard
plan: 3
type: execute
wave: 2
depends_on: [1]
files_modified: [apps/web/src/app/(dashboard)/timeline/page.tsx, apps/web/src/components/timeline/*, apps/web/src/app/api/control/inject/route.ts, apps/web/src/components/inject-button.tsx]
autonomous: false
requirements: [DASH-03, SIM-05]
must_haves:
  truths:
    - "The color-coded production timeline renders state bands per machine over the shift window, live"
    - "The Inject Breakdown button causes a visible cascade: timeline band turns red, andon tile reddens, OEE waterfall availability drops — within seconds"
    - "A human has watched the full cascade and signed off"
  artifacts:
    - path: "apps/web/src/components/timeline/gantt.tsx"
      expected: "ECharts custom-series Gantt timeline"
  key_links:
    - from: "inject-button"
      to: "simulator /control/inject-breakdown"
      via: "POST /api/control/inject proxy (web never talks MQTT/DB-write)"
---

<objective>
The vertical-slice closer: the production timeline (the one flagged-risk ECharts task — custom series Gantt) and the Inject Breakdown demo control, ending with a human-verified live cascade. This plan runs parallel to 03-02 (different files); the human checkpoint at the end verifies the WHOLE Phase-3 slice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/STACK.md (§ ECharts custom series — front-loaded risk; spike first)
@.planning/phases/03-live-dashboard/03-01-PLAN.md (timeline API + useLive)
</context>

<tasks>

<task type="auto">
  <name>Task 1: ECharts Gantt spike → timeline component</name>
  <files>apps/web/src/components/timeline/gantt.tsx, apps/web/src/app/(dashboard)/timeline/page.tsx</files>
  <action>
    SPIKE FIRST (timeboxed ~1h): a minimal ECharts `custom` series rendering 3 hardcoded state bands on a time x-axis; commit it as the component's test fixture story. Then the real component:
    - y categories = machines of the selected line; x = time (sim) clamped to shift window; each state_interval row → `renderItem` rect filled with the shared `stateColor(state)` token; open interval extends to sim_now and keeps growing live.
    - Tooltip: state, reason label, duration (sim-minutes), 'INJECTED' tag when injected=true.
    - Breaks shaded as hatched background bands (from shift calendar via API metadata) so viewers see PPT semantics visually.
    - `dataZoom` x-axis slider; refetch on useLive.
    Page: line + shift pickers consistent with the OEE page.
  </action>
  <verify>Component test with a fixture of 6 intervals renders 6 rects with correct token colors; live page shows bands matching /api/timeline data.</verify>
  <done>DASH-03 rendered from real derived intervals.</done>
</task>

<task type="auto">
  <name>Task 2: Inject Breakdown control (SIM-05)</name>
  <files>apps/web/src/app/api/control/inject/route.ts, apps/web/src/components/inject-button.tsx</files>
  <action>
    - `POST /api/control/inject {lineId}` → server-side fetch to `http://simulator:4000/control/inject-breakdown` (env SIMULATOR_URL) — web stays a thin proxy (ARCHITECTURE boundary; browser cannot reach the simulator container directly).
    - `inject-button.tsx`: prominent red 'Inject breakdown' button (line-aware: on andon tile hover + on timeline page header). Confirm-less but rate-limited client-side (disabled 10s after use). Toast: 'Breakdown injected on Line 2 — watch it cascade'.
    - Mount the button on the andon tiles and the timeline page (coordinate with 03-02's tile component via a small render-slot prop if 03-02 already merged; else add to both here and note in SUMMARY).
  </action>
  <verify>Clicking the button on L3: /api/timeline shows a new DOWN interval with injected=true within 2s; andon tile turns red.</verify>
  <done>The demo's dramatic beat is wired end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human walkthrough — the vertical slice cascade</name>
  <files>(verification only)</files>
  <read_first>
    - .planning/phases/03-live-dashboard/03-02-SUMMARY.md (if present — screens built in the sibling plan)
  </read_first>
  <action>
    Present this walkthrough and PAUSE for the reply. Prereq: `docker compose up -d`, wait ~60s, open http://localhost:3000.
    1. Andon: all 4 lines visible with plausible mixed states; counts climbing; sim clock ticking at 60×.
    2. OEE: pick L1 (showcase) — OEE near 85%; pick L4 (problem) — OEE below 45%; waterfall identity holds (PPT − losses = productive).
    3. Timeline: L2 shows alternating green/red/amber bands with break shading.
    4. THE CASCADE: click 'Inject breakdown' on Line 2 → within ~5 real seconds: timeline grows a red band (tooltip shows INJECTED) → andon L2 tile turns red with reason → OEE page for L2: availability visibly drops on refetch.
    5. Kill and restart `docker compose restart worker` → dashboard recovers live updates within ~15s (SSE reconnect).
    Reply "approved" or list failing step numbers (observed vs expected).
  </action>
  <verify><automated>MANUAL — human checkpoint. Automated proxies already green: 03-01 curl SSE check, Task 1–2 verifies.</automated></verify>
  <resume-signal>Type "approved" or describe issues per step.</resume-signal>
  <done>Human sign-off recorded in SUMMARY (or defect list routed to a gap plan).</done>
</task>

</tasks>

<verification>
- Component + proxy tests green; human cascade sign-off obtained.
</verification>

<success_criteria>
Phase 3 success criteria complete: andon/waterfall/timeline live, inject-breakdown cascades visibly within seconds, human-verified. The demo's core 30 seconds now exists.
</success_criteria>

<output>
Write `.planning/phases/03-live-dashboard/03-03-SUMMARY.md` with the sign-off outcome.
</output>
