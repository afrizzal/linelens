---
phase: 03-live-dashboard
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: [apps/web/src/lib/listener.ts, apps/web/src/app/api/stream/route.ts, apps/web/src/app/api/andon/route.ts, apps/web/src/app/api/oee/route.ts, apps/web/src/app/api/timeline/route.ts, apps/web/src/app/api/sim-clock/route.ts, apps/web/src/hooks/use-live.ts]
autonomous: true
requirements: []
must_haves:
  truths:
    - "GET /api/stream is a working SSE endpoint: nodejs runtime, force-dynamic, 15s keep-alive ping, clean close on client abort — verified INSIDE docker, not just next dev"
    - "One shared pg LISTEN connection fans out to all SSE clients via an in-process EventEmitter; web never writes to Postgres"
    - "use-live hook: EventSource with automatic reconnect and a 5s polling fallback"
  artifacts:
    - path: "apps/web/src/app/api/stream/route.ts"
      expected: "SSE route with ReadableStream, ping, abort cleanup"
  key_links:
    - from: "worker NOTIFY 'linelens'"
      to: "browser EventSource"
      via: "pg LISTEN → EventEmitter → SSE"
---

<objective>
Build the live plumbing: worker NOTIFY (already emitting from 02-02) → web's dedicated LISTEN connection → EventEmitter → SSE route → client hook, plus the three read-model API routes the dashboard pages will consume. Infrastructure only — no UI in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/ARCHITECTURE.md (§ SSE fan-out — implement that design; poll+diff is the documented fallback)
@.planning/research/PITFALLS.md (SSE-in-docker, proxy buffering)
@.planning/STATE.md (blocker note: confirm Next.js SSE mechanics via Context7 at phase start — do that first; if Context7 is unavailable, verify against next.js official docs via WebFetch and note it)
</context>

<tasks>

<task type="auto">
  <name>Task 1: LISTEN singleton + SSE route</name>
  <files>apps/web/src/lib/listener.ts, apps/web/src/app/api/stream/route.ts</files>
  <action>
    - `listener.ts`: globalThis-cached singleton (AIDA db.ts pattern): one dedicated `pg.Client` (NOT a pool client) → `LISTEN linelens` → parse payload JSON → `emitter.emit('change', payload)`. Reconnect with backoff on connection error. Export `getEmitter()`.
    - `route.ts` (`GET /api/stream`): `export const runtime='nodejs'; export const dynamic='force-dynamic';` Return `new Response(readableStream, { headers: { 'Content-Type':'text/event-stream', 'Cache-Control':'no-cache, no-transform', 'Connection':'keep-alive', 'X-Accel-Buffering':'no' } })`. In `start(controller)`: subscribe to emitter → `controller.enqueue('event: change\ndata: '+JSON.stringify(payload)+'\n\n')`; ping comment `: ping\n\n` every 15s; on `request.signal` abort: clear interval, unsubscribe, close. Optional `?lineId=` filter.
  </action>
  <verify>Inside docker (`docker compose up`): `curl -N http://localhost:3000/api/stream` prints pings and, while the sim runs, change events within ~1s of derivation batches. Ctrl-C then check web logs: no listener leak (subscriber count returns to 0 — log it).</verify>
  <done>Live change feed works in the real deployment shape.</done>
</task>

<task type="auto">
  <name>Task 2: Read-model API routes</name>
  <files>apps/web/src/app/api/andon/route.ts, apps/web/src/app/api/oee/route.ts, apps/web/src/app/api/timeline/route.ts</files>
  <action>
    Read-only routes (Prisma client from @linelens/db; web NEVER writes — ENG-01 single-writer discipline):
    - `/api/andon`: per line: current open state_interval (state + reasonCode + since), Σ goodDelta this shift (good count), and target count = `Σ over the line's machines of floor(PPT_elapsed_sec / machine_current_ICT)` — machines produce IN PARALLEL, so the line target is the SUM of per-machine ideal counts (a single-ICT line formula would undercount target ~2× and pin the andon progress bar >100%). Document in code: this is a demo pacing target, not a claimed standard.
    - `/api/sim-clock`: `{simNow, speed}` from `SELECT sim_now()` + SimClock row — built HERE in wave 1 because both wave-2 UI plans (03-02 top bar, 03-03 live timeline edge) consume it.
    - `/api/oee?lineId&shiftDate&shiftId`: rows from v_line_shift_oee + v_machine_shift_oee incl. waterfall buckets (a_loss/p_loss/q_loss minutes), availability/performance/quality/oee, ict_misconfigured, plus 'N/A' semantics: return nulls as nulls (UI renders N/A — never 0).
    - `/api/timeline?lineId&shiftDate&shiftId`: state_interval rows clamped to the shift window: {machineId, state, start, end|null, reasonCode, injected}.
    - All: `dynamic='force-dynamic'`; current sim shift resolved via a small `simNow()` query helper (`SELECT sim_now()`).
  </action>
  <verify>With sim running: each route returns valid JSON matching the running plant (spot-check andon states against `mosquitto_sub` output).</verify>
  <done>Dashboard has everything it needs to render from three GETs.</done>
</task>

<task type="auto">
  <name>Task 3: use-live client hook</name>
  <files>apps/web/src/hooks/use-live.ts</files>
  <action>
    `useLive(refetch: () => void, opts?: {lineId?})`: opens EventSource('/api/stream'), on 'change' → debounce 300ms → refetch(); on error → close, retry connect with backoff (1s→5s cap); while disconnected, poll refetch() every 5s (the ARCHITECTURE fallback, automatic). Return {connected}. Keep it dependency-free (no SWR) — pages hold state with plain fetch + this hook.
  </action>
  <verify>Component test (vitest + jsdom, EventSource mocked): change event triggers one debounced refetch; killing the source flips to polling; reconnect stops polling.</verify>
  <done>One hook gives every page live updates with graceful degradation.</done>
</task>

</tasks>

<verification>
- In-docker curl SSE check green; routes green; hook tests green; `pnpm typecheck` clean.
- Grep: no Prisma `.create/.update/.delete` under apps/web (read-only discipline).
</verification>

<success_criteria>
Worker-to-browser live pipeline proven inside compose; read models expose exactly the verified view semantics (nulls stay nulls). UI plans can now be pure presentation.
</success_criteria>

<output>
Write `.planning/phases/03-live-dashboard/03-01-SUMMARY.md` (note whether Context7 confirmation happened or the WebFetch fallback was used).
</output>
