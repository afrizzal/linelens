---
phase: 03-live-dashboard
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified: [apps/web/src/app/(dashboard)/*, apps/web/src/components/andon/*, apps/web/src/components/oee/*, apps/web/src/components/ui/*, apps/web/src/app/globals.css]
autonomous: true
requirements: [DASH-01, DASH-04]
must_haves:
  truths:
    - "The andon board shows ALL lines on one dark screen — state color, good count, target count — updating live without reload"
    - "The OEE waterfall renders A×P×Q with time-bucket losses per line/shift; N/A states render as 'N/A', never 0%"
    - "ict_misconfigured shows a visible warning badge (domain literacy on display)"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/andon/page.tsx"
      expected: "live andon board page"
    - path: "apps/web/src/app/(dashboard)/oee/page.tsx"
      expected: "OEE waterfall page with line/shift picker"
  key_links: []
---

<objective>
The first visible screens: navigation shell, the live andon board (the "living plant" proof), and the OEE waterfall. English UI. Dark, TV-on-the-factory-floor aesthetic. ECharts enters here (waterfall) — keep chart code isolated in components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/FEATURES.md (§ UX baseline: andon minimum = state+good+target — verified Vorne pattern)
@.planning/phases/03-live-dashboard/03-01-PLAN.md (routes + hook consumed here)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shell + design tokens</name>
  <files>apps/web/src/app/(dashboard)/layout.tsx, apps/web/src/app/globals.css, apps/web/src/components/ui/*</files>
  <action>
    - Dark-first factory theme via CSS variables (Tailwind): background near-black `#0B0F14`, panel `#121821`, text `#E6EDF3`; state colors AS TOKENS used by every screen: `--state-execute: #22C55E` (green), `--state-down: #EF4444` (red), `--state-changeover: #F59E0B` (amber), `--state-break: #64748B` (slate). One `stateColor(state)` helper — components NEVER hardcode state hexes (the timeline/andon/Pareto must agree).
    - `(dashboard)/layout.tsx`: left nav (LineLens wordmark + Activity icon; items: Andon, OEE, Timeline, Orders (disabled 'Phase 4' badge), DDS (disabled)); top bar: sim clock display (sim date+time, speed badge e.g '60×') polled from a tiny `/api/sim-clock` route (add it here), and a live-connection dot from useLive's `connected`.
    - Basic UI atoms: Card, Badge, Select (line/shift pickers) — hand-rolled + Tailwind, no component library (keep deps lean).
  </action>
  <verify>App renders shell with nav + live sim clock ticking (advances ~1 sim-min per real second at 60×).</verify>
  <done>Navigable shell with shared state-color tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Andon board (DASH-04)</name>
  <files>apps/web/src/app/(dashboard)/andon/page.tsx, apps/web/src/components/andon/*</files>
  <action>
    Client page: fetch `/api/andon` + `useLive(refetch)`. Grid of line tiles (auto-fit, min 280px). Tile: line name; giant state word (RUNNING/DOWN/CHANGEOVER/BREAK) tinted with the state token + subtle pulsing ring when DOWN; reason label when DOWN/CHANGEOVER (e.g. 'Mechanical jam · 4m 12s' — duration in SIM time from `since` vs /api/sim-clock, ticked client-side at sim rate); good count vs target count as '{good} / {target}' with a thin progress bar (verified Vorne TAED-style minimum: state + good + target). Machines strip: small per-machine state dots. Full-screen mode (hide nav via CSS) — this is the TV screen.
  </action>
  <verify>With sim running: all 4 lines visible; a natural DOWN event turns a tile red within ~2s without reload; counts climb.</verify>
  <done>The living-plant money view exists.</done>
</task>

<task type="auto">
  <name>Task 3: OEE waterfall (DASH-01)</name>
  <files>apps/web/src/app/(dashboard)/oee/page.tsx, apps/web/src/components/oee/waterfall.tsx, apps/web/src/components/oee/kpi-tiles.tsx</files>
  <action>
    Page: line picker + shift picker (current shift default; from /api/oee metadata) + `useLive`. 
    - KPI tiles: Availability / Performance / Quality / OEE as percentages (1 dp). NULL → 'N/A' in muted style (ENG-05 semantics; NEVER render 0%). `ict_misconfigured` → amber badge 'Check Ideal Cycle Time' with tooltip 'Performance >100% usually means ICT is set too high' (verbatim domain literacy).
    - Waterfall (ECharts bar): categories `[Planned Production Time, Availability Loss, Performance Loss, Quality Loss, Fully Productive Time]`; implement as stacked-bar waterfall (invisible offset series + visible series — standard ECharts pattern): PPT full bar; each loss step drops; final bar = PPT×OEE. Values in MINUTES (time-bucket fields a_loss/p_loss/q_loss from /api/oee) with % annotations. Colors: losses use the A/P/Q palette (red/amber/violet `#8B5CF6`), productive green.
    - Below: per-machine mini-table (A/P/Q/OEE per machine, N/A-safe).
    ECharts: `echarts` + thin React wrapper component (init once, `setOption` on data change, dispose on unmount, `renderer:'canvas'`).
  </action>
  <verify>Waterfall totals reconcile: PPT − a − p − q = productive (assert in a component test with fixture data); against the running sim, the identity holds for the current shift within rounding.</verify>
  <done>The A×P×Q story renders correctly and live.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` + component tests green; manual: andon + waterfall both live-update while `docker compose up` runs.
- N/A rendering check: query a future shift → tiles show N/A (not 0%).
</verification>

<success_criteria>
DASH-01 + DASH-04 satisfied with the verified UX minimums; state colors single-sourced; the screens look like a product, not a chart demo.
</success_criteria>

<output>
Write `.planning/phases/03-live-dashboard/03-02-SUMMARY.md`.
</output>
