---
phase: 04-difot-pareto-dds
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified: [packages/db/src/views.sql, packages/db/prisma/migrations/*, apps/web/src/app/(dashboard)/orders/[id]/page.tsx, apps/web/src/app/api/orders/[id]/route.ts, apps/worker/test/causality.test.ts]
autonomous: true
requirements: [DIFOT-02]
must_haves:
  truths:
    - "From a late/at-risk order, one click reaches the ranked machine-level loss events that starved its production window — with estimated lost units per loss"
    - "The link is causal, not cosmetic: injecting a breakdown on a different line changes WHICH orders go late (test-proven)"
    - "The headline sentence renders: 'Line 2 breakdown 14:20–15:05 cost ~180 units → this order shipped 120 short of schedule'"
  artifacts:
    - path: "packages/db/src/views.sql"
      expected: "order_loss_drilldown SQL function"
  key_links:
    - from: "customer_order"
      to: "loss_event"
      via: "product → line(s) → loss windows overlapping [orderDate, max(dueDate, shippedAt|sim_now)]"
---

<objective>
The money shot: the OEE→DIFOT drill-down. This single screen is the project's verified-safe differentiator ("OEE tools stop at the machine; supply-chain analytics starts at the order; LineLens demonstrates the bridge"). It must be causally honest and provable by test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/ARCHITECTURE.md (§ drill-down query — implement that SQL function)
@.planning/phases/04-difot-pareto-dds/04-01-PLAN.md (order model)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drill-down SQL function</name>
  <files>packages/db/src/views.sql, packages/db/prisma/migrations/*</files>
  <action>
    `order_loss_drilldown(order_id)` (SQL function, on-read — data volume is tiny):
    1. Resolve order → productId → line(s) whose machines run that product (machine.currentProductId OR any COUNTS event for that product in the window — use the events; product rotation means multiple lines possible).
    2. Window = `[orderDate, COALESCE(shippedAt, GREATEST(dueDate, sim_now()))]`.
    3. Select loss_event rows on those lines overlapping the window; per row compute `est_lost_units`:
       - AVAILABILITY & PERFORMANCE losses: `lost_time_sec / product.ideal_cycle_time_sec` (units the line would have produced in that lost time);
       - QUALITY losses: `lost_units` directly.
    4. Return ranked by est_lost_units DESC, limit 12: {category, reasonLabel, machineId, lineId, windowStart, windowEnd, lostTimeSec, estLostUnits, injected}.
    5. Also return the order's shortfall context: `short_units = qtyOrdered − allocated_at_due` (0 if shipped on time) and `total_est_lost_units` — the drill-down headline compares these: losses ≥ shortfall makes the story arithmetically coherent.
  </action>
  <verify>SQL test on a fixture database: known losses + known short order → function returns them ranked with correct est_lost_units (hand-computed: 45-min breakdown at ICT 60s → 45 est lost units).</verify>
  <done>The bridge query exists and is auditable SQL.</done>
</task>

<task type="auto">
  <name>Task 2: Order detail drill-down UI</name>
  <files>apps/web/src/app/(dashboard)/orders/[id]/page.tsx, apps/web/src/app/api/orders/[id]/route.ts</files>
  <action>
    Complete the order detail page:
    - Header: customer, product, qty progress bar (allocated/ordered), due vs shipped (sim times), status chip.
    - THE HEADLINE (for LATE/AT_RISK): auto-composed sentence from the top loss + shortfall: `"{LineName} {top.reasonLabel} {HH:mm}–{HH:mm} cost ~{estLostUnits} units → this order is {short} short of schedule."` — template in one function with unit tests (English copy; this exact sentence style is the case-study quote).
    - Loss list: ranked cards — category badge (Six-Big-Losses color language), reason, machine, sim window, lost time, est units; 'INJECTED' tag when injected. Each card links to the Timeline page pre-filtered (`/timeline?lineId&shiftDate` + highlight param — add highlight support to the timeline page: pulse the matching band).
    - ON_TIME orders show the fulfillment story instead ('Produced on L1 across 2 shifts, shipped 3h early') — the contrast sells the bridge.
  </action>
  <verify>Component test: LATE fixture renders headline + ranked losses; ON_TIME fixture renders fulfillment story. Live: clicking a loss card lands on the timeline with the band highlighted.</verify>
  <done>One click from broken promise to machine cause.</done>
</task>

<task type="auto">
  <name>Task 3: Causality proof test</name>
  <files>apps/worker/test/causality.test.ts</files>
  <action>
    The DIFOT-02 acceptance test (pure pipeline, Testcontainers): seed 42, two scenario runs of the same 2 sim-days:
    - Run A: baseline (no injection) → record which orders are LATE.
    - Run B: same seed + injected 90-min breakdown on line L2 mid-day-1 → assert: (i) at least one order for L2's product that was ON_TIME in Run A is now LATE/short; (ii) orders on other lines' products keep their Run A status; (iii) drill-down for the newly-late order ranks the injected breakdown in its top 3 losses.
    This test is the honesty backbone of the whole differentiator claim — if it can't pass, the feature is cosmetic and must be fixed, not shipped.
  </action>
  <verify>Test green.</verify>
  <done>The bridge is provably causal.</done>
</task>

</tasks>

<verification>
- SQL, component, and causality tests green.
- Live demo check: inject on L2 → within ~1 sim-day an L2-product order shows LATE with the injected breakdown top-ranked in its drill-down.
</verification>

<success_criteria>
DIFOT-02 done: the money shot works, is causally test-proven, and produces the exact sentence the case study will quote.
</success_criteria>

<output>
Write `.planning/phases/04-difot-pareto-dds/04-02-SUMMARY.md`.
</output>
