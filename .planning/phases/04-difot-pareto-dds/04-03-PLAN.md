---
phase: 04-difot-pareto-dds
plan: 3
type: execute
wave: 2
depends_on: [1]
files_modified: [apps/web/src/app/(dashboard)/losses/page.tsx, apps/web/src/components/losses/pareto.tsx, apps/web/src/app/(dashboard)/dds/page.tsx, apps/web/src/app/api/losses/route.ts, apps/web/src/app/api/dds/route.ts, apps/worker/src/dds/actions.ts]
autonomous: true
requirements: [DASH-02, DDS-01]
must_haves:
  truths:
    - "The losses Pareto ranks Six Big Losses reasons by lost time, stackable per shift, with cumulative-% line"
    - "The DDS screen shows yesterday's (sim) S/Q/D summary + OEE + top loss, exactly top-3 actions with owners, and escalation status"
    - "DDS actions are deterministic rule-generated from yesterday's ledger — no LLM, no randomness without seed"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/dds/page.tsx"
      expected: "the management screen"
  key_links: []
---

<objective>
The management layer: the Six Big Losses Pareto (DASH-02, verified as THE dominant loss-presentation pattern) and the Daily Direction Setting screen (DDS-01, per the verified DDS content spec: yesterday's performance, today's top-3 actions, escalation). Runs parallel with 04-02 (different files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/00-domain-research.md (§6 DDS spec — 10–15-min meeting content; do NOT claim IWS/P&G tiering)
@.planning/phases/02-oee-engine/02-03-PLAN.md (v_loss_pareto feeds the Pareto)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Losses Pareto (DASH-02)</name>
  <files>apps/web/src/app/(dashboard)/losses/page.tsx, apps/web/src/components/losses/pareto.tsx, apps/web/src/app/api/losses/route.ts</files>
  <action>
    - `/api/losses?lineId&day`: v_loss_pareto rows grouped by reasonCode with per-shift breakdown.
    - Page: enable the 'Losses' nav item (03-02 shipped it disabled with a Phase-4 badge). Line picker + sim-day picker. ECharts combo: bars = lost minutes per reason (sorted desc), STACKED by shift (S1/S2 shades — the verified 'Pareto stackable per shift' pattern), category color per Six Big Loss family; right axis line = cumulative % (classic Pareto). Toggle: 'By reason' / 'By category'. Tooltip: reason label, category, minutes, est units (Σ lostUnits where present). Empty state: 'No losses recorded yet — let the plant run.'
    - Reuse the chart wrapper from 03-02; useLive refetch.
  </action>
  <verify>Component test: fixture reasons render sorted with cumulative line reaching 100%; stack toggle by shift sums to the same totals.</verify>
  <done>The dominant loss view exists, stackable per shift.</done>
</task>

<task type="auto">
  <name>Task 2: DDS aggregation + rule-generated actions</name>
  <files>apps/web/src/app/api/dds/route.ts, apps/worker/src/dds/actions.ts</files>
  <action>
    `/api/dds?lineId?` (plant-wide default) for 'yesterday' = last COMPLETED sim-day:
    - Safety: `daysSinceIncident` = seeded counter (seed 42 → start 147) + 1 per completed sim-day, reset only by a scripted incident (none in v1) — labeled 'synthetic' in the payload; UI shows it plainly (honesty: it's demo data, never claim a safety record).
    - Quality: yesterday plant Q% + total rejects (from v_machine_shift_oee aggregated).
    - Delivery: yesterday DIFOT% + late order count (v_difot).
    - OEE: yesterday plant OEE + delta vs day before — PLANT aggregation is sums-of-components across ALL machines (A = Σrun/ΣPPT, P = Σict_time/Σrun, Q = Σgood/Σtotal, OEE = A×P×Q — same rule as v_line_shift_oee, NEVER an average of line ratios); top loss = max lostTimeSec (category+reason+line).
    - Actions (`actions.ts`, pure function, unit-tested): top-3 reasons by yesterday lost time → deterministic TWO-LEVEL template map so EVERY reason code always resolves: (1) specific overrides — BRK-MECH → 'Schedule mechanical inspection on {line}' owner 'Maintenance — Pak Andi'; CO-OVERAGE → 'Run changeover SMED review on {line}' owner 'Production — Bu Sari'; SS-MISFEED → 'Check feeder alignment on {line}' owner 'Operator lead — Pak Budi'; SL-SPEED → 'Audit rated-speed settings' owner 'Engineering — Pak Dwi'; RJ-STARTUP → 'Review warm-up parameters' owner 'Quality — Bu Rina'; (2) CATEGORY fallbacks covering every other code (BRK-ELEC/BRK-SENSOR/SS-MATERIAL/SS-SENSOR/RJ-DIM/RJ-VISUAL/CO-PRODUCT/CO-CLEAN): UNPLANNED_STOPS → 'Investigate {reasonLabel} on {line}' owner 'Maintenance — Pak Andi'; PLANNED_STOPS → 'Review changeover procedure on {line}' owner 'Production — Bu Sari'; SMALL_STOPS → 'Investigate recurring {reasonLabel} on {line}' owner 'Operator lead — Pak Budi'; SLOW_CYCLES → 'Audit line speed on {line}' owner 'Engineering — Pak Dwi'; STARTUP_REJECTS/PRODUCTION_REJECTS → 'Review {reasonLabel} defects on {line}' owner 'Quality — Bu Rina'. Unit test: every reason code in contracts resolves to an action+owner. Owners = seeded fixed roster (English UI, Indonesian names — plausible plant).
    - Escalation rules (deterministic): any single loss ≥ 45 sim-min OR line DIFOT < 80% yesterday (from `v_difot_line`, built in 04-01) → escalation item {line, reason, metric}; else 'No escalations'.
  </action>
  <verify>Unit tests: fixture ledger → exactly 3 actions in lost-time order with mapped owners; escalation triggers at thresholds; determinism (two runs identical).</verify>
  <done>DDS content per the verified spec, deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: DDS screen</name>
  <files>apps/web/src/app/(dashboard)/dds/page.tsx</files>
  <action>
    Enable the nav item. Layout = a board a supervisor reads in 10 minutes (the verified meeting artifact):
    - Top strip: three S/Q/D tiles (Safety: days-since-incident with 'synthetic data' microcopy; Quality: Q% + rejects; Delivery: DIFOT% + late count) + yesterday OEE tile with delta arrow.
    - 'Top loss yesterday' card: category badge, reason, line, lost minutes → links to Losses Pareto filtered to yesterday.
    - 'Today's top 3 actions' list: numbered, action + owner chip + source reason.
    - 'Escalations' card: items or the green all-clear.
    - Footer microcopy: 'Daily Direction Setting — inspired by TPM daily management practice.' (inspired-by wording only — no IWS/P&G claims, per §8 guardrail).
  </action>
  <verify>With ≥1 completed sim-day (warm start guarantees it): screen renders all cards with real data; links land filtered.</verify>
  <done>DDS-01 complete — the screen that says 'management system', not 'chart demo'.</done>
</task>

</tasks>

<verification>
- All tests green; live screen check for both pages; typecheck clean.
</verification>

<success_criteria>
DASH-02 + DDS-01 done with verified semantics and honest labeling. Phase 4 complete → the full demo storyline exists end-to-end.
</success_criteria>

<output>
Write `.planning/phases/04-difot-pareto-dds/04-03-SUMMARY.md`.
</output>
