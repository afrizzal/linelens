---
phase: 04-difot-pareto-dds
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: [packages/db/prisma/schema.prisma, packages/db/prisma/migrations/*, apps/worker/src/orders/*, apps/web/src/app/(dashboard)/orders/page.tsx, apps/web/src/app/api/orders/route.ts]
autonomous: true
requirements: [DIFOT-01]
must_haves:
  truths:
    - "A seeded order book is generated per sim-day, sized so healthy lines mostly ship on time and breakdowns push real orders late (~85–95% of expected capacity)"
    - "Good production allocates FIFO to open orders by due date; an order ships when fully allocated; DIFOT % = shipped in-full AND on-time / orders due"
    - "Order statuses: ON_TIME / LATE / AT_RISK (projected finish past due) / OPEN"
  artifacts:
    - path: "apps/worker/src/orders/allocate.ts"
      expected: "FIFO allocation from good deltas to open orders"
  key_links:
    - from: "customer_order + allocation"
      to: "loss_event"
      via: "line/product/time-window join (consumed by 04-02 drill-down)"
---

<objective>
Build the order side of the bridge: order generation, FIFO allocation of good production, DIFOT computation, and the Orders screen. The causal chain must be real — a breakdown reduces good output, which delays specific order completions — because 04-02's drill-down depends on it being true, not cosmetic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/ARCHITECTURE.md (§ order book / DIFOT — implement that model)
@docs/00-domain-research.md (§5 DIFOT framing — KPI semantics + honest positioning)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema + order generation</name>
  <files>packages/db/prisma/schema.prisma, packages/db/prisma/migrations/*, apps/worker/src/orders/generate.ts</files>
  <action>
    Models:
    - `CustomerOrder { id, productId, customer, qtyOrdered Int, orderDate DateTime /*sim*/, dueDate DateTime /*sim*/, shippedAt DateTime?, @@index([productId, dueDate]) }`
    - `Allocation { id, orderId, lineId, machineId, qty Int, producedAt DateTime /*sim*/, sourceEventId BigInt, @@unique([orderId, sourceEventId]) }` (idempotent per COUNTS event)
    Generation (worker job, runs when a new sim-day starts, seeded rng from contracts + sim-day as salt → deterministic):
    - Per product: expected daily capacity = Σ over machines currently running that product of `(PPT_per_day_sec / ICT) × profileOeeEstimate` where profileOeeEstimate = {showcase: .85, typical: .58, problem: .40} (a documented planning heuristic, not a claim).
    - Daily demand = capacity × uniform(0.85, 1.00), split into 3–6 orders (qty lognormal-ish via triangular), customers from a seeded list of 8 Indonesian-flavored B2B names (e.g. 'PT Surya Component', 'CV Delta Plastik'), orderDate = day start, dueDate = day end + 1 sim-day (lead time).
    - Backfill: on first run, also generate for the already-simulated warm-start day(s) so DIFOT/DDS have history.
  </action>
  <verify>Two runs over the same sim-days produce identical orders (determinism). Demand/capacity ratio in [0.85, 1.0].</verify>
  <done>A believable, deterministic order book exists.</done>
</task>

<task type="auto">
  <name>Task 2: FIFO allocation + shipping + status</name>
  <files>apps/worker/src/orders/allocate.ts</files>
  <action>
    Hook into the derivation scan (02-02 single pass): for each COUNTS event with goodDelta>0: allocate goodDelta units to open orders of that productId ordered by (dueDate, id) — FIFO by due date (ARCHITECTURE: 'simplest credible model'). Write Allocation rows (idempotent unique key). When an order's Σqty reaches qtyOrdered in the same transaction: set shippedAt = event simTime.
    Status (SQL/view or computed in API — put in a view `v_order_status`):
    - SHIPPED + shippedAt ≤ dueDate → ON_TIME; SHIPPED late → LATE; OPEN with dueDate < sim_now() → LATE (open-late); OPEN with projected_finish > dueDate → AT_RISK else OPEN. projected_finish = sim_now() + remaining_qty × ICT / max(ε, current_good_rate) where current_good_rate = good units/sec over the trailing 60 sim-min on that product's line(s).
    - `v_difot`: per sim-day (by dueDate): DIFOT% = count(ON_TIME) / count(due that day). In-full is implied by the ship rule (ship only when fully allocated) — state this in a comment (DIFOT-01's 'in-full' semantics).
  </action>
  <verify>Fixture test: capacity 100/day, demand 90 across 3 orders, inject a 30%-output day → exactly the earliest-due orders ship, the last order flips LATE; allocations never exceed qtyOrdered; replay-idempotent.</verify>
  <done>Causally real fulfillment with correct DIFOT math.</done>
</task>

<task type="auto">
  <name>Task 3: Orders screen</name>
  <files>apps/web/src/app/(dashboard)/orders/page.tsx, apps/web/src/app/api/orders/route.ts</files>
  <action>
    - `/api/orders?day=`: v_order_status rows + v_difot summary + per-line DIFOT contribution.
    - Page (enable the nav item): DIFOT KPI tile ('DIFOT 78% · 14/18 orders on time, in full' + trend vs yesterday); orders table: customer, product, qty (allocated/ordered progress), due (sim), status chip (ON_TIME green / AT_RISK amber pulsing / LATE red / OPEN slate — reuse state-color language). Row click → `/orders/[id]` (stub detail page: order facts + allocations list; 04-02 completes it with the drill-down). useLive refetch.
  </action>
  <verify>Against the running sim: at least one AT_RISK or LATE order exists on the problem line's product within 1 sim-day; DIFOT % matches hand count of the table.</verify>
  <done>DIFOT-01 visible and live.</done>
</task>

</tasks>

<verification>
- Fixture + determinism tests green; live check: inject-breakdown on a line, wait ~1 sim-day → that line's product shows lower DIFOT contribution.
</verification>

<success_criteria>
Orders exist, fulfillment is causally coupled to production, DIFOT % is correct and live (DIFOT-01 done). 04-02 can now make the link explorable.
</success_criteria>

<output>
Write `.planning/phases/04-difot-pareto-dds/04-01-SUMMARY.md`.
</output>
