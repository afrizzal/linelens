---
phase: 05-distribution
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: [README.md, LICENSE, scripts/smoke.sh, scripts/demo-scenario.ts, docs/assets/*, .github/workflows/ci.yml]
autonomous: false
requirements: [DIST-01, DIST-02]
must_haves:
  truths:
    - "A stranger can clone, `cp .env.example .env`, `docker compose up`, and see the living plant — proven by a clean-machine smoke script, not by 'works on my machine'"
    - "README: hero GIF at the top, one-command quick start, architecture diagram, honest 'What this is / isn't' section"
    - "A 60-second seeded demo GIF captures inject-breakdown → andon → OEE → DIFOT drill-down"
    - "Repo is public on github.com/afrizzal with MIT license"
  artifacts:
    - path: "README.md"
      expected: "hero GIF + quick start + architecture + honesty section"
    - path: "scripts/demo-scenario.ts"
      expected: "deterministic demo: fixed seed + scripted inject timeline"
  key_links: []
---

<objective>
Make LineLens public and provable: deterministic demo path, clean-clone smoke test, star-worthy README with the money-shot GIF, MIT license, public repo. This phase IS the product for the portfolio audience — treat polish as a requirement, not garnish.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md (§ honesty guardrail, § positioning — the README language rules)
@.planning/research/PITFALLS.md (§ distribution failure mode — the reason this phase exists)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic demo scenario + smoke script + CI</name>
  <files>scripts/demo-scenario.ts, scripts/smoke.sh, .github/workflows/ci.yml</files>
  <action>
    - `demo-scenario.ts`: `pnpm demo` — brings compose up with env overrides `SIM_SPEED=120` (seed stays the plant.config default 42; warm-start is already unconditional per 01-03 — there is NO separate DEMO flag), waits for healthy, then a scripted timeline via the control API: t+30s inject L2 breakdown (240 sim-min — long enough to flip an order late), prints the cue card ('Now open Orders — watch ORD-xxx go AT_RISK'). Deterministic: same run, same story, every time (GIF + live demos + recruiter walkthroughs all use this).
    - `smoke.sh`: clean-clone proof — in a temp dir: `git clone . && cp .env.example .env && docker compose up -d --build`, poll: db+mqtt healthy, simulator /healthz, web /api/andon returns 4 lines, machine_event count increasing across 2 samples, THEN `docker compose down -v`. Exit non-zero on any failure. Runs on the local machine (WSL2/Git Bash compatible — verify path handling on Windows host; note deviations).
    - `ci.yml`: GitHub Actions — pnpm install, typecheck, unit tests (worker fixture tests included; skip Testcontainers-heavy suites behind a label if runner-time hurts; document what runs where).
  </action>
  <verify>`bash scripts/smoke.sh` passes locally from a clean temp clone; CI green on first push.</verify>
  <done>The demo is reproducible and stranger-proof.</done>
</task>

<task type="auto">
  <name>Task 2: README + LICENSE</name>
  <files>README.md, LICENSE, docs/assets/architecture.svg</files>
  <action>
    MIT LICENSE (Afrizzal, 2026). README structure (English, top to bottom):
    1. `# LineLens` + one-liner: 'Real-time OEE analytics with a built-in factory simulator — see how machine downtime becomes broken customer promises.'
    2. HERO GIF placeholder (Task 3 fills it) + 3 bullet 'What you're seeing'.
    3. Quick start: 3 commands (clone / cp .env.example .env / docker compose up) + 'open http://localhost:3000'. `pnpm demo` for the scripted story.
    4. 'The bridge' section: the verified positioning sentence — OEE tools stop at the machine; supply-chain analytics starts at the order; LineLens demonstrates the bridge in one drill-down. NO absolute-uniqueness claim (§8 guardrail).
    5. Feature tour (andon/waterfall/timeline/Pareto/DIFOT drill-down/DDS — one screenshot each, captured from the seeded demo).
    6. Architecture: simple SVG diagram (simulator → MQTT → worker → Postgres → SSE → dashboard) + 'Industry grounding' box: preferred OEE calculation, Six Big Losses, PackML-inspired states, Sparkplug-B-style topics, UNS-style hierarchy — each with its caveat wording ('inspired', 'style', never 'compliant'/'ISA-95-aligned').
    7. 'What this is / What this isn't': simulated build informed by manufacturing experience at Integra + PLC background; synthetic data; not a production MES; calibration knobs are engineering estimates, not sourced industry claims.
    8. Roadmap (v2: waste module, Markov predictive maintenance, real ingestion), License, Author (link porto + LinkedIn).
  </action>
  <verify>README renders clean on GitHub (check after push); every claim traces to the honesty rules; quick start commands copy-paste-work per smoke.sh.</verify>
  <done>Star-worthy front door with honest framing.</done>
</task>

<task type="checkpoint:human-assist" gate="blocking">
  <name>Task 3: GIF capture + repo publish</name>
  <files>docs/assets/hero.gif, docs/assets/*.png</files>
  <action>
    Human-in-the-loop (recording + publishing need the user):
    1. Run `pnpm demo`. Record with ScreenToGif (or LICEcap): the 60-second money shot — andon all-green-ish → click Inject breakdown L2 → tile reds → OEE waterfall availability drops → Orders: an order flips AT_RISK → click → drill-down shows the injected breakdown top-ranked → headline sentence visible. Target ≤10MB (GitHub renders inline; 800px wide, 10–12 fps).
    2. Capture 6 stills (one per feature-tour section), light polishing pass on any visual glitches found while recording (file as gap items if non-trivial).
    3. Publish: `gh repo create afrizzal/linelens --public --source . --push` (user runs `gh auth login` first if needed). Confirm README + GIF render on github.com.
    PAUSE for the user at each step needing their hands/accounts; provide exact commands.
  </action>
  <verify><automated>MANUAL — capture + publish. Automated proxy: smoke.sh green pre-publish.</automated></verify>
  <resume-signal>User confirms: GIF recorded, repo public, README renders.</resume-signal>
  <done>github.com/afrizzal/linelens is live with the hero GIF.</done>
</task>

</tasks>

<verification>
- smoke.sh green from clean clone; CI green; repo public; GIF renders in README (DIST-01, DIST-02 complete).
</verification>

<success_criteria>
A stranger's 60 seconds: land on the repo → GIF tells the downtime→late-order story → quick start actually works. The StockLens demo-gap failure mode is structurally closed.
</success_criteria>

<output>
Write `.planning/phases/05-distribution/05-01-SUMMARY.md`.
</output>
