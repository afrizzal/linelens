---
phase: 05-distribution
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified: [docs/case-study.md, docs/linkedin-draft.md]
autonomous: false
requirements: [DIST-03, DIST-04]
must_haves:
  truths:
    - "A portfolio case study exists with honest framing and is published on afrizzal.pro (following the porto deploy mechanism)"
    - "One LinkedIn process post is published (drafted via the user's linkedin-post playbook)"
    - "LineLens is attached to at least one real job application"
  artifacts:
    - path: "docs/case-study.md"
      expected: "case study source (porto integration happens in the porto repo)"
  key_links: []
---

<objective>
Close the loop that past projects never closed: the case study, the LinkedIn story, and a real application. Mostly human-gated — the executor drafts, the user publishes. IMPORTANT for the executor: work OUTSIDE this repo (porto site) follows that repo's own rules — read the referenced memory files before touching it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md (§ context: distribution gap, positioning, honesty)
<note>
Porto site work MUST follow `C:\Users\MANAGER-IT\.claude\projects\D--Aff-proj\memory\porto-deploy-mechanism.md` (index.php = base64 of index.html; regenerate after edit). LinkedIn drafting follows the user's `linkedin-post` skill/playbook. Job application uses the cover-letter pipeline in `D:\Aff\proj\career\03-cover-letters`.
</note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Case study draft</name>
  <files>docs/case-study.md</files>
  <action>
    Write the case study (English, mirrors StockLens case-study structure on the porto):
    - Problem: 'Plant managers see OEE; sales sees late orders; nobody sees the wire between them.'
    - The demo story: the exact drill-down headline sentence from 04-02, with the GIF.
    - How it's built: architecture + industry grounding (verified definitions, calibrated realism, speed-invariance testing — engineering depth is the differentiator for an IT-leadership audience).
    - Honesty box (verbatim framing): simulated build informed by manufacturing experience at Integra (Dynamics AX, multi-site) + PLC background (Omron); bridge positioning, no uniqueness claim.
    - Cross-links: StockLens (batch analytics sibling), MarkovLens (future predictive maintenance engine), AIDA (product engineering).
    - CTA: repo + 'run it in one command'.
  </action>
  <verify>Draft reads in ≤3 minutes; every claim passes the honesty rules; sentences quoted from the app match the actual UI copy.</verify>
  <done>Publishable case-study source exists.</done>
</task>

<task type="checkpoint:human-assist" gate="blocking">
  <name>Task 2: Publish case study + LinkedIn post + application attach</name>
  <files>docs/linkedin-draft.md</files>
  <action>
    With the user, in order:
    1. Porto: integrate case-study into afrizzal.pro following porto-deploy-mechanism memory (READ IT FIRST; regenerate index.php after edit). Add LineLens to the projects grid.
    2. LinkedIn: draft `docs/linkedin-draft.md` via the linkedin-post playbook (process-story angle: 'I built a factory simulator to prove a point about late orders' — hook per playbook formulas, Indonesian voice, no-brand guardrails). User reviews, edits, posts. Record the post URL.
    3. Application: attach the repo + case study to ≥1 live application (candidate: manufacturing-sector roles from /linkedin-jobs; check with the user which application is active this week). Update `D:\Aff\proj\career\00-strategy\job-leads.md` status.
    PAUSE for the user at each publish/send step.
  </action>
  <verify><automated>MANUAL — publishing requires the user's accounts and judgment.</automated></verify>
  <resume-signal>User confirms: case study live, post published (URL), application sent (which one).</resume-signal>
  <done>DIST-03 + DIST-04 complete with URLs/records in the SUMMARY.</done>
</task>

</tasks>

<verification>
- Case study live on afrizzal.pro; LinkedIn post URL recorded; ≥1 application includes LineLens.
</verification>

<success_criteria>
v1 milestone complete: built AND distributed. Update PROJECT.md requirements to Validated where shipped; the retrospective question for /gsd:complete-milestone: did distribution produce any inbound signal within 2 weeks?
</success_criteria>

<output>
Write `.planning/phases/05-distribution/05-02-SUMMARY.md` with all URLs and the application record.
</output>
